import { Layout } from 'antd';

const { Footer } = Layout;

const AppFooter = () => {
  return (
    <Footer style={{ textAlign: 'center' }}>
      Este es el pie de página de la tienda online de supermercado.
    </Footer>
  );
};

export default AppFooter;
